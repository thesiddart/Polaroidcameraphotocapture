
  # Polaroid Camera Photo Capture

  This is a code bundle for Polaroid Camera Photo Capture.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
